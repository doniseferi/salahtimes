import { iterativeTest } from '../../testUtils/index';
/* 
    Property-based testing:
    Test contract: Preconditions, post conditions and invariance.
*/
describe('TimeSpan', () => {
    test('returns the value in milliseconds', () => {
        
    })
})
