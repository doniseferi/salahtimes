language: node_js
os: linux
node_js:
  - "lts/*"

after_success: npm run coverage

deploy:
  provider: npm
  api_token: $NPM_TOKEN
  edge: true
  access: public
  cleanup: true
  on:
    branch: master
